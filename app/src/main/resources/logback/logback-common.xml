<configuration>
    <property name="MDC_PATTERN" value="service=%X{service_name} request_id=%X{request_id} correlation_id=%X{correlation_id} trace_id=%X{trace_id} method=%X{http_method} uri=%X{request_uri} client_ip=%X{client_ip}"/>
    <property name="CONSOLE_LOG_DEV_PATTERN" value="%d{HH:mm:ss.SSS} [%thread] %highlight(%-5level) %cyan(%logger{36}) ${MDC_PATTERN} - %msg%n"/>
    <property name="CONSOLE_LOG_PROD_PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} ${MDC_PATTERN} - %msg%n"/>
</configuration>
